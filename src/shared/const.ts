export const COOKIE_NAME = "app_session_id";
export const ONE_YEAR_MS = 1000 * 60 * 60 * 24 * 365;

/** localStorage key for compare drawer persisted snapshots — bump when breaking CompareSnapshot shape. */
export const COMPARE_STORE_STORAGE_KEY = "ktm-compare-v1";
