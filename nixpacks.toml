[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g corepack@0.24.1", "corepack enable", "corepack prepare pnpm@10.4.1 --activate", "pnpm i --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
